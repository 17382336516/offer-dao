# 通义千问（DashScope）密钥，复制本文件为 .env 并填入你的真实 Key
# 获取地址：https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=sk-your-real-key-here

# Vercel 前端部署时必填：指向 Render 后端地址（前端所有 /api 请求都会打到此处）。
# 本地开发留空即可，Vite devServer 会走 proxy 转到 127.0.0.1:3000。
# 若 Vercel 控制台漏配此变量，前端会回退到下方默认地址（https://offer-dao.onrender.com）。
VITE_API_BASE_URL=https://offer-dao.onrender.com

# 可选：指定千问模型。默认 qwen-plus；免费额度内更快更省可用 qwen-turbo
# 可选值：qwen-turbo（快/便宜，免费额度覆盖）、qwen-plus（均衡）、qwen-max（最强，可能不覆盖免费额度）
# QWEN_MODEL=qwen-turbo
